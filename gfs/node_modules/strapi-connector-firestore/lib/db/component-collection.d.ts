import type { FirestoreConnectorModel } from '../model';
import type { Collection } from './collection';
export declare class ComponentCollection<T extends object = never> implements Collection<T> {
    readonly model: FirestoreConnectorModel<T>;
    private collection;
    constructor(model: FirestoreConnectorModel<T>);
    private throw;
    get converter(): never;
    get path(): never;
    autoId(): string;
    doc(): never;
    get(): never;
    where(): never;
    orderBy(): never;
    limit(): never;
    offset(): never;
}
